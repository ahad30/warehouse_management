import { node } from "prop-types";

const InstallationRouteCopy = ({ children }) => {
  return children;
};

InstallationRouteCopy.propTypes = {
  children: node,
};

export default InstallationRouteCopy;
